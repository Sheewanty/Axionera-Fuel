/**
 * Login layout — minimal shell, no sidebar or header.
 * The root globals.css is imported by the root layout, so tokens are available.
 */
export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
